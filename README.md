# Student-Management-System
A simple, human-friendly web application to manage student records. Built with Java Servlets, JDBC, MySQL, and plain HTML/CSS. The project demonstrates a clean structure, CRUD operations (Create, Read, Update, Delete).
